public class GeneralNotes {
    /*declaring arrays
     type variableName[] = {};
    or
     type variableName[] = new type[size]*/

    /*
    top hat questions

    -put first 5 prime values into an array
    int prime[] = {2,3,5,7,11};

    //put the integer 10 into the elements of the array values with the lowest and highest valid index
    int[] values = new int[10]
    values[0] = 10;
    values[values.length - 1] = 10;
     */

}
